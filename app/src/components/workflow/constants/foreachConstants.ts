// Blocked sub-task types are shared across container editors — see
// subtaskConstants.ts. These aliases preserve the original foreach/group import
// names.
import { SUBTASK_BLOCKED_TYPES } from './subtaskConstants';

export const FOREACH_SUBTASK_BLOCKED_TYPES = SUBTASK_BLOCKED_TYPES;
export const GROUP_SUBTASK_BLOCKED_TYPES = SUBTASK_BLOCKED_TYPES;
