{{- if .Values.hasuraClickhouseDataConnector.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "hasuraClickhouseDataConnector.fullname" . }}
  labels:
    {{- include "hasuraClickhouseDataConnector.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.dataConnectorReplicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "hasuraClickhouseDataConnector.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
      {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "hasuraClickhouseDataConnector.selectorLabels" . | nindent 8 }}
      {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      {{- with (( default .Values.global.imagePullSecrets .Values.imagePullSecrets )) }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "hasuraClickhouseDataConnector.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.imageClickhouseDataConnector.repository }}:{{ .Values.imageClickhouseDataConnector.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.imageClickhouseDataConnector.pullPolicy }}
          envFrom:
            {{- if .Values.secretNameClickhouseDataConnector }}
            - secretRef:
                name: {{ .Values.secretNameClickhouseDataConnector }}
            {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.dataClickhouseConnectorPort }}
              protocol: TCP
          resources:
            {{- toYaml .Values.resourcesClickhouseDataConnector | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}