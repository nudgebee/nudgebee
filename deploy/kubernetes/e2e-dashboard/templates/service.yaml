apiVersion: v1
kind: Service
metadata:
  name: {{ include "e2e-dashboard.fullname" . }}
  labels:
    {{- include "e2e-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "e2e-dashboard.selectorLabels" . | nindent 4 }}
