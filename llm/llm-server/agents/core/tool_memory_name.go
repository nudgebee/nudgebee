package core

// ToolMemory names the on-demand memory search tool (path B). Kept in this OSS
// file (no ee/memory import) so OSS code compiles when the EE implementation
// (tool_memory.go) is stripped — see .oss-exclude.
const ToolMemory = "memory"
