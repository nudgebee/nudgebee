table:
  name: spends_resource_group_aggregate
  schema: public
select_permissions:
  - role: tenant_admin
    permission:
      columns:
        - count
        - resource_group
        - amount
        - date
        - tenant
        - business_unit
      filter:
        tenant:
          _eq: X-Hasura-User-Tenant-Id
