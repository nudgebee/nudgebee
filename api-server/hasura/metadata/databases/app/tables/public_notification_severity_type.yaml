table:
  name: notification_severity_type
  schema: public
is_enum: true
array_relationships:
  - name: notifications
    using:
      foreign_key_constraint_on:
        column: severity
        table:
          name: notifications
          schema: public
select_permissions:
  - role: tenant_admin
    permission:
      columns:
        - value
        - description
      filter: {}
      allow_aggregations: true
  - role: tenant_admin_readonly
    permission:
      columns:
        - value
        - description
      filter: {}
      allow_aggregations: true
