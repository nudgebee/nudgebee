table:
  name: tenant_type
  schema: public
is_enum: true
array_relationships:
- name: tenants
  using:
    foreign_key_constraint_on:
      column: type
      table:
        name: tenant
        schema: public
